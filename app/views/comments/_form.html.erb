<%= form_with model: [target, comment], url: url  do |form| %>
  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body, required: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
